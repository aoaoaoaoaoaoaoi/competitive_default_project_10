//並び替え全列挙
int array[]={0,1,2,3,4,5,6,7};
  do{
    string str="";
      for(int i=0; i<(int)s.size(); i++){
          str += array[i];
      }
      cout << str << endl;
  }while(next_permutation(array,array+(int)s.size()));